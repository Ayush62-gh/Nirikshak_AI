# Core OCR engine
paddlepaddle==3.0.0
paddleocr==2.10.0

# Image processing (cv2, PIL used by paddle_ocr, preprocess, quality_checker)
opencv-python
Pillow
numpy

# API server
fastapi
uvicorn[standard]
python-multipart

# Testing (not deployed, but needed for CI)
pytest
